import numpy as np 

def random_swap(vec, percent, distribution, scalar, seed):
    
    nvals = len(vec)
    rng = np.random.default_rng(seed)

    ind_2_swap = rng.choice()

def random_sum(vec,percent,distribution,scalar,seed):
